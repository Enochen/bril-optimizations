extract = 'total_dyn_inst: (\d+)'
benchmarks = '**/*.bril'

[runs.baseline]
pipeline = ["bril2json", "brili -p {args}"]

[runs.tdce]
pipeline = ["bril2json", "cargo run --bin dce", "brili -p {args}"]
